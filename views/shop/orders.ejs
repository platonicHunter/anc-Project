<!-- <%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/orders.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if (orders.length <= 0) { %>
            <div class="empty-state">
                <h1>Nothing here!</h1>
            </div>
        <% } else { %>
            <ul class="orders">
                <% orders.forEach(order => { %>
                    <li class="order">
                        <div class="order-header">
                            <h2>Order #<%= order._id %></h2>
                            <a href="/orders/<%= order._id %>" class="invoice-link">View Invoice</a>
                        </div>
                        <ul class="products">
                            <% order.products.forEach(p => { %>
                                <li class="product">
                                    <div class="product-details">
                                        <h3><%= p.product.title %></h3>
                                        <p>Quantity: <%= p.quantity %></p>
                                    </div>
                                    <div class="product-image">
                                        <img src="/<%= p.product.imageUrl %>" alt="<%= p.product.title %>">
                                    </div>
                                </li>
                            <% }); %>
                        </ul>
                    </li>
                <% }); %>
            </ul>
        <% } %>
    </main>
    <%- include('../includes/end.ejs') %>
</body> -->

<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/orders.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if (orders.length <= 0) { %>
            <div class="empty-state">
                <h1>Nothing here!</h1>
            </div>
        <% } else { %>
            <ul class="orders">
                <% orders.forEach(order => { %>
                    <li class="order">
                        <div class="order-header">
                            <h2>Order #<%= order._id %></h2>
                            <a href="/orders/<%= order._id %>" class="invoice-link">View Invoice</a>
                        </div>
                        <ul class="products">
                            <% order.products.forEach(p => { %>
                                <li class="product">
                                    <div class="product-details">
                                        <h3><%= p.product.title %></h3>
                                        <p>Quantity: <%= p.quantity %></p>
                                    </div>
                                    <div class="product-image">
                                        <img src="/<%= p.product.imageUrl %>" alt="<%= p.product.title %>">
                                    </div>
                                </li>
                            <% }); %>
                        </ul>
                    </li>
                <% }); %>
            </ul>
        <% } %>
    </main>
    <%- include('../includes/end.ejs') %>
</body>
